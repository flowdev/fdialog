#!/usr/bin/env -S fdialog run --file
version 1

window main(width=400, height=300) {
    dialog error(
        type="error",
        message="Don't do that again!",
        buttonText="OK, please don't yell.",
        width=400, height=300
    ) {
        action close(type="exit", code=0)
    }
}
